FROM ubuntu:latest

# to run docker file - docker run -t -i --rm ubuntu bash
# to execute command - docker run -t -i --rm ubuntu bash -c "echo hello"
# to stop container - docker stop <container_id>
# to remove container - docker rm <container_id>
# to remove image - docker rmi <image_id>
